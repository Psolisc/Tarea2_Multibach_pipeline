pipeline {
    agent {
        label 'linux'
    }

    stages {
        stage('Ejecutar Python Script') {
            steps {
                sh "python Nested Triangle_Patricia Solis.py"
            }
        }
    }
}
