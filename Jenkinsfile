pipeline {
    agent {
        label 'windows'
    }

    stages {
        stage('Ejecutar Python Script') {
            steps {
                powershell "python Nested Triangle_Patricia Solis.py"
            }
        }
    }
}
