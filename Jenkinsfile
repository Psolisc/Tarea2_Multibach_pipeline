pipeline {
    agent {
        label 'any'
    }

    stages {
        stage('Ejecutar Python Script') {
            steps {
                shell "python Nested Triangle_Patricia Solis.py"
            }
        }
    }
}
