pipeline {
    agent {
        label 'any'
    }

    stages {
        stage('Ejecutar Python Script') {
            steps {
                sh "python NestedTriangle.py"
            }
        }
    }
}
