pipeline {
    agent {
        label 'windows'
    }

    stages {
        stage('Ejecutar Python Script') {
            steps {
                bat "python Nested Triangle_Patricia Solis.py"
            }
        }
    }
}
